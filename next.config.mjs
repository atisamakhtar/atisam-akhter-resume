/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['images.unsplash.com', 'tailwindui.com'],  // You can add multiple domains
    },
  };
  
export default nextConfig;
  